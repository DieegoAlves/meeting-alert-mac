# F-054 — Sem suíte de testes; pipeline de alertas é lacuna de verificação

## F-054 | categoria: funcional | severidade: baixa | status: needs-human
- Tela: Package.swift:16-41 — nenhum `.testTarget`; diretório `Tests/` inexistente
- Passos: 1) Rodar `swift test` na raiz do pacote. 2) Nenhum alvo de teste é executado. 3) `AlertPlanner`, `SnoozeState` e `EventStore` são value types puros (testáveis com fixtures mock, sem credenciais), mas não há um único teste.
- Esperado: Comportamento crítico — timing de alerta (T-5/T-1), colapso de overlaps, snooze/suppress, windowing do EventStore, exclusão de all-day (F-050) — coberto por testes que rodem offline.
- Observado: Zero testes no pacote. Toda validação ponta-a-ponta do pipeline (`CalendarSyncService`, `GoogleCalendarAPI`, `GoogleAuth`) exige credenciais Google OAuth vivas (Client ID + token) e não há stub de `AuthProviding`/`GoogleCalendarAPI` — LACUNA DE VERIFICAÇÃO explícita para este track. Regressões em timing/overlap/snooze passam despercebidas antes do release.
- (resolvedor) Correção: needs-human — por quê: escrever uma suíte de testes é um item de trabalho próprio, não parte desta integração serializada (que tinha escopo definido: F-042/F-053/F-052 + reconciliação + build/medição/docs). Adicionar um `.testTarget` + fixtures + validar `swift test` é meaningful e arrisca o entregável de "build release limpo" desta etapa. Nada bloqueia a implementação — os tipos críticos são value types puros e testáveis offline sem credenciais. PLANO PRONTO para o humano/próxima etapa:
  1. `Tests/MeetingAlertTests/` + `.testTarget(name: "MeetingAlertTests", dependencies: ["Scheduler", "Core"])` em Package.swift.
  2. `AlertPlannerTests`: exclusão de all-day (F-050), colapso de overlaps coincidentes, filtro por `disabledCalendarIDs` (F-053), self-decline, stale-grace, ordering de `nextGroup`.
  3. `SnoozeStateTests`: fired/suppressed/snooze override + `prune`.
  4. `EventStoreTests`: windowing [now-7d … fim de amanhã], `apply(upserts:removedIDs:)` (upsert+remove por id, F-040), `upcoming`/`history`/`active`.
  Nenhum stub de rede necessário para esses; a cobertura de `CalendarSyncService`/`GoogleCalendarAPI`/`GoogleAuth` exigiria mocks de `AuthProviding` + URLProtocol e fica para um segundo momento. Nenhuma mudança de código feita nesta etapa para F-054.
