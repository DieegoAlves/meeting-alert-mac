# F-065 — Distribuição: DMG assinado ad-hoc + GitHub Release

## F-065 | categoria: distribuição/CI | severidade: alta | status: in-progress
- Objetivo (Diego): distribuir um DMG no GitHub para OUTRAS pessoas instalarem, assinado
  **ad-hoc** (sem conta Apple Developer). Client ID + Secret do Google embutidos no build.
- Telas/arquivos: `scripts/gen-secrets.sh` (novo), `scripts/package-dmg.sh` (novo),
  `Sources/Core/Secrets.swift` (GERADO, gitignored), `Sources/Core/OAuthClientConfig.swift` (novo),
  `Sources/Auth/GoogleAuth.swift`, `Sources/App/DemoFixtures.swift`, `Sources/App/AppDelegate.swift`,
  `Sources/Prefs/PreferencesView.swift`, `.github/workflows/{ci,release}.yml`, `.gitignore`,
  `README.md`, `LICENSE`.

## Pipeline (sequencial)
1. Credenciais embutidas em build time (env → Secrets.swift), NUNCA commitadas + override
   "Usar meu próprio client OAuth" nas Preferências.
2. Empacotamento local: `scripts/package-dmg.sh` (Release universal, .app, ad-hoc sign, DMG, SHA256).
3. Git + repo público `meeting-alert-mac` + secrets do repo (gh secret set).
4. GitHub Actions: `release.yml` (tag v*) + `ci.yml` (build sem secrets).
5. Release v1.0.0 verde + DMG anexado + SHA256 conferido.
6. Docs: README PT-BR público + LICENSE MIT.

## Regras
- Nunca commitar/imprimir segredo (provado por grep de `GOCSPX-` e `apps.googleusercontent.com`).
- Só mexer em `meeting-alert-mac/`.
