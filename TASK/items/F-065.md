# F-065 — Distribuição: DMG assinado ad-hoc + GitHub Release

## F-065 | categoria: distribuição/CI | severidade: alta | status: fixed
- **Release v1.0.0 PUBLICADA:** https://github.com/DieegoAlves/meeting-alert-mac/releases/tag/v1.0.0
  — DMG anexado (MeetingAlert-1.0.0.dmg, 1.63 MB), título "MeetingAlert 1.0.0", não-draft.
  SHA256 do DMG baixado DA Release conferido = 8ae44ecc54691af8b80ad4051da2b380a6f5d18da721b47a142dc93cfffbdbbf ✅.
  Publicada a partir do build local (via dist/publish.sh) porque o GitHub Actions está travado por
  billing; o release.yml continua no repo para tags futuras (quando o billing for resolvido).
  Segurança reconferida: nenhum GOCSPX- em nenhuma revisão do git; Secrets.swift/credentials.json/dist gitignored.
- Objetivo (Diego): distribuir um DMG no GitHub para OUTRAS pessoas instalarem, assinado
  **ad-hoc** (sem conta Apple Developer). Client ID + Secret do Google embutidos no build.
- Repo público: **https://github.com/DieegoAlves/meeting-alert-mac**

## Entregue
1. **Credenciais embutidas em build time** — `scripts/gen-secrets.sh` gera
   `Sources/Core/Secrets.swift` (GITIGNORED) a partir de `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`.
   Resolver central `Sources/Core/OAuthClientConfig.swift`: embutido por padrão + override
   **"Usar meu próprio client OAuth"** (Preferências). Sem embutido e sem override → instrução
   clara (`AuthError.missingClientID`), nunca crash. `GoogleAuth`, `DemoFixtures`, `AppDelegate`,
   `PreferencesView` integrados. Build release limpo.
2. **`scripts/package-dmg.sh`** — Release universal (arm64+x86_64 via `--arch`), `.app` com
   Info.plist versionado (CFBundleShortVersionString do arg, bundle id estável, LSUIElement,
   ícone `AppIcon.icns` gerado por `scripts/make-icns.sh`), codesign ad-hoc
   (`--force --deep --options runtime --identifier com.disco-tec.MeetingAlert --sign -`),
   DMG (hdiutil UDZO) com atalho `/Applications` + LEIA-ME + SHA256.
3. **Git + repo** — `.gitignore` (build/, .build/, dist/, DerivedData, credenciais, Secrets.swift,
   .DS_Store); commit inicial limpo; repo PÚBLICO criado e `main` empurrado.
   Secrets do repo `GOOGLE_CLIENT_ID` e `GOOGLE_CLIENT_SECRET` setados via pipe (nunca impressos),
   lidos de `~/Library/Containers/com.disco-tec.MeetingAlert/.../credentials.json` (app é sandboxed).
4. **GitHub Actions** — `.github/workflows/ci.yml` (build sem segredos em push/PR) e
   `release.yml` (tag `v*` → package-dmg.sh com secrets → Release com DMG + SHA256). Empurrados.
5. **Teste de instalação local** — DMG universal montado, `.app` copiado p/ `/Applications`,
   quarentena removida, app ABRE e RODA (phys_footprint 17 MB, dentro do orçamento <30 MB).
   Gatekeeper: `spctl` = **rejected** (esperado: ad-hoc/não-notarizado → 1ª abertura exige
   botão direito → Abrir, documentado no README). DMG local COM creds reais:
   `SHA256 = 8ae44ecc54691af8b80ad4051da2b380a6f5d18da721b47a142dc93cfffbdbbf` (GOCSPX embutido ✓).
6. **Docs** — README público PT-BR (o que faz, instalação, quarentena/notarização, Conectar
   Google + aviso de "Testing"/usuários de teste, seção Dev, Privacidade) + `LICENSE` MIT.

## BLOQUEADO (fora do meu contrato de worker)
- **Release v1.0.0 não publicada.** O push de tag de versão / criar GitHub Release é **ato
  HUMANO** (release-gate do Overclock bloqueou `git push origin v1.0.0`). Diego deve empurrar a tag.
- **GitHub Actions travado por billing.** O 1º run de CI falhou com: *"The job was not started
  because your account is locked due to a billing issue."* → nem CI nem `release.yml` rodam até
  o Diego resolver o billing da conta GitHub. Depois disso, empurrar a tag dispara a Release.

## Ação do Diego para OUTRAS pessoas logarem (Google, modo "Testing")
Google Cloud Console (projeto do app) → **APIs e Serviços → Tela de consentimento OAuth →
Usuários de teste → + Add Users** → e-mail da pessoa → Salvar. Até **100** testadores. Sem isso,
quem não estiver na lista recebe "Acesso bloqueado / app não verificado".

## Segurança
- Grep de `GOCSPX-<valor>` e do Client ID nos arquivos rastreados: **nenhum secret** versionado
  (só menções ao padrão de grep em docs + o Client ID público em notas históricas). Secrets.swift
  gitignored; dist/ gitignored. Nenhum valor de secret impresso em log/handoff.
