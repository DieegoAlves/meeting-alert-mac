# F-063 — Botão "Testar alerta" (dispara o overlay real com evento sintético)

## F-063 | categoria: UX/Dev | severidade: baixa | status: fixed
- Telas: `Sources/Prefs/PreferencesView.swift` (seção "Som de alerta"), `Sources/MenuBar/MenuBarView.swift` (menu da barra), `Sources/Scheduler/AlertScheduler.swift` (dono do caminho de disparo), `Sources/App/AppDelegate.swift` (fiação).
- Pedido (Diego): "add a test button for the alert" — validar o alerta cheio sem esperar uma reunião real, exercitando o CAMINHO REAL (não um overlay paralelo).

## Implementação (root) — FIXED
Reaproveita o caminho de disparo REAL: `AlertScheduler.fireTestAlert()` monta APENAS um `CalendarEvent` sintético e chama `presenter.present(_:)` — exatamente o método que `handleFire()` usa num alerta T-1 verdadeiro. Mesmos painéis por monitor (`.screenSaver`, todos os monitores), mesmo som configurado, mesma contagem regressiva, mesmos atalhos de teclado e mesma fiação de ações. Nenhum overlay duplicado.

- **Evento sintético**: id reservado `meeting-alert.test-alert`, título **"Reunião de teste"**, `start = agora`, link `https://meet.google.com/test` (provider .meet). Nunca entra no `EventStore`.
- **Ações no teste** (roteadas pelas MESMAS closures → `AlertScheduler`), com desvio de teste no topo de cada método:
  - **Entrar / ↩** → abre `https://meet.google.com/test` e fecha.
  - **Adiar 1/3/5** → fecha e re-dispara um overlay de TESTE após o intervalo (via `testSnoozeTimer` dedicado — o planner real nunca re-dispararia um evento fora do store).
  - **Depois / Esc** → fecha.
  - **Já estou na call / ␣** → fecha.
  - NENHUMA toca `SnoozeState` real, o cache de eventos (`EventStore`) nem o histórico (histórico = eventos passados derivados do store; as ações nunca escrevem no store). IDs sintéticos que porventura entrassem no `SnoozeState` seriam podados no próximo `arm()` (não estão no store) — mas o desvio de teste evita até isso.
- **Sem órfão**: fechar reusa `presenter.dismissActiveOverlay()` → `tearDownOverlay()`, que já destrói TODOS os painéis (`contentViewController = nil`, `close()`), remove monitor de teclado e observer de Space e para o som. `endTest()`/`fireTestAlert()`/`disarm()` cancelam o `testSnoozeTimer` pendente → nenhuma janela nem timer órfão.
- **Entradas**:
  - Preferências → seção **"Som de alerta"** → botão **"Testar alerta"** (posta `PreferencesEvents.testAlertNotification`).
  - Menu da barra → item **"Testar alerta…"** (posta `MenuBarController.testAlertNotification`).
  - `AppDelegate` observa AMBAS e chama `scheduler.fireTestAlert()`. `AlertScheduling` (contrato Core congelado) NÃO foi alterado — `fireTestAlert()` é método concreto do `AlertScheduler`.

## Validação
- `swift build -c release` **VERDE**; `.app` empacotada, assinada ad-hoc, relançada UMA vez, `background only = true`.
- **phys_footprint idle (antes/depois) = 18 MB** (orçamento < 30 MB) — `./scripts/measure-footprint.sh` PASS. `fireTestAlert` só acrescenta um evento sintético + um timer cancelado no fechamento, então o footprint volta ao valor idle de 18 MB após o teste (mesmo `tearDownOverlay` já validado em perf/F-062).
- **Limite do ambiente**: disparar o botão, exercitar cada ação clicando e medir a memória PÓS-dismiss exige clicar no item da barra/Preferências, o que precisa de permissão de Acessibilidade que o osascript não tem aqui (mesmo `-25211` do F-062). O caminho de disparo/teardown é o MESMO `present()`/`tearDownOverlay()` que o modo demo já exercita automaticamente. Diego pode confirmar clique-a-clique (Entrar abre o link, Adiar re-dispara, Esc/Depois/Já estou na call fecham) — nada toca eventos reais nem histórico.
