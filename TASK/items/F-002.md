# F-002 — measure-footprint.sh usa `sudo footprint`; sem sudo cai pra peak-RSS e reporta FALHA falsa

## F-002 | categoria: performance | severidade: alta | status: fixed
- Tela: scripts/measure-footprint.sh:121 ; scripts/measure-footprint.sh:118 ; scripts/measure-footprint.sh:187 ; scripts/measure-footprint.sh:195
- Passos:
  1) Rodar `scripts/measure-footprint.sh` sem credencial sudo em cache (CI / agente / shell sem TTY).
  2) Observar o veredito impresso vs o phys_footprint real (20 MB, ver F-001).
- Esperado: como o budget é phys_footprint (research/03:257), o script deveria medir phys_footprint via `footprint` (que NÃO precisa de sudo para o processo do próprio usuário — comprovado em F-001, rodei `footprint -p MeetingAlert` sem sudo e obtive 20 MB) e aprovar.
- Observado: o script exige `sudo` e, quando falha, troca silenciosamente de métrica (phys_footprint → RSS) mas continua comparando contra o budget de phys_footprint → FALHA garantida e falsa. Trechos citados:
  - scripts/measure-footprint.sh:121 → `fp_raw=$(sudo footprint -p MeetingAlert 2>/dev/null || true)` — o `sudo` é desnecessário; `2>/dev/null || true` engole o erro.
  - scripts/measure-footprint.sh:118 → `PHYS_MB="N/A"` (default) permanece porque `fp_raw` fica vazio.
  - scripts/measure-footprint.sh:187-192 → com `PHYS_MB == "N/A"`, `CHECK_VAL="$PEAK_RSS_MB"` (passa a checar RSS).
  - scripts/measure-footprint.sh:195-196 → `VERDICT = (RSS < 30) ? PASS : FAIL` → RSS ~59.8/61.6 MB > 30 → FAIL, contra um budget que a própria linha 8 declara ser phys_footprint.
  Este é o mecanismo exato que gerou o "59.8 MB" do builder.
- Custo pro usuário: nenhum direto; mas o gate de QA mente (diz que o app falha o budget quando passa), fazendo perder esforço "otimizando" um app já aprovado.
- (resolvedor) Correção:

Correção sugerida: (1) remover `sudo` — tentar `footprint -p MeetingAlert` direto, só escalar pra sudo se vier vazio; (2) NÃO cair pra RSS contra budget de phys_footprint — se não obtiver phys_footprint, sair com status "não medido" (≠ FAIL); (3) imprimir os DOIS números rotulados ("phys_footprint (budget): X" e "RSS (informativo, inclui páginas compartilhadas): Y"); (4) tirar o `2>/dev/null || true` da medição pra falha de ferramenta aparecer. Relacionado a F-001.

- (integração serializada) FIXED em scripts/measure-footprint.sh:
  1. `sudo` removido — `footprint -p MeetingAlert` roda direto (funciona para o processo do próprio usuário) e o stderr NÃO é mais silenciado.
  2. O julgamento agora usa SÓ phys_footprint. Se `footprint` falhar, o script imprime `RESULT: UNMEASURED` e sai com código 2 (distinto de PASS=0 / FAIL=1) — nunca mais um FAIL falso comparando RSS contra um budget de phys_footprint.
  3. Sempre imprime os dois números rotulados: "phys_footprint (budget metric)" e "RSS peak (informational, incl. shared COW pages)".
  4. Cabeçalho do script corrigido (não diz mais "requires sudo" / "fallback RSS"). Nenhuma mudança em Sources/. Relacionado a F-001.
  5. BUG DE PARSING adicional descoberto ao re-rodar e também corrigido: o script fazia `grep "physical footprint:"` (label do `vmmap`) mas a ferramenta `footprint` imprime `phys_footprint:` (na seção "Auxiliary data") — nunca casava → PHYS_MB ficava "N/A" mesmo com sudo removido. Além disso o formato é `phys_footprint: 21 MB` (valor e unidade em campos SEPARADOS), então o antigo `awk '{print $NF}'` pegava "MB" em vez de "21". Corrigido: grep passou a `phys_footprint:`, e a extração agora pega o campo após o `:` e separa número/unidade (aceita `MB/KB/GB`). Re-rodado: `RESULT: PASS  phys_footprint = 21.00 MB` (budget < 30), CPU idle 0.00%.
